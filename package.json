{
  "name": "metrics-api",
  "version": "1.0.0",
  "private": true,
  "description": "Hardware metrics API for Raspberry Pi",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "start": "node server.js --",
    "dev": "DEV_MODE=true nodemon server.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "express": "^5.2.1",
    "express-rate-limit": "^8.3.2",
    "jsonwebtoken": "^9.0.3"
  },
  "devDependencies": {
    "nodemon": "^3.1.14"
  }
}
